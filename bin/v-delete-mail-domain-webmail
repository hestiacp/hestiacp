#!/bin/bash
# info: delete mail domain antivirus support
# options: USER DOMAIN
#
# The function disables clamav scan for incoming emails.


#----------------------------------------------------------#
#                    Variable&Function                     #
#----------------------------------------------------------#

# Argument definition
user=$1
domain=$2
domain_idn=$2

# Includes
source $HESTIA/func/main.sh
source $HESTIA/func/domain.sh
source $HESTIA/conf/hestia.conf

# Additional argument formatting
format_domain
format_domain_idn

#----------------------------------------------------------#
#                    Verifications                         #
#----------------------------------------------------------#

check_args '2' "$#" 'USER DOMAIN'
is_format_valid 'user' 'domain'
is_system_enabled "$MAIL_SYSTEM" 'MAIL_SYSTEM'
is_object_valid 'user' 'USER' "$user"
is_object_unsuspended 'user' 'USER' "$user"
is_object_valid 'mail' 'DOMAIN' "$domain"
is_object_unsuspended 'mail' 'DOMAIN' "$domain"
is_object_value_exist 'mail' 'DOMAIN' "$domain" '$WEBMAIL'
is_object_value_empty 'mail' 'DOMAIN' "$domain" '$WEBMAIL'

#----------------------------------------------------------#
#                       Action                             #
#----------------------------------------------------------#

# Delete webmail configuration files
del_webmail_config

#----------------------------------------------------------#
#                       Hestia                             #
#----------------------------------------------------------#

# Delete webmail in config
update_object_value 'mail' 'DOMAIN' "$domain" '$WEBMAIL' 'no'

# Restart services
$BIN/v-restart-mail $restart
$BIN/v-restart-service $IMAP_SYSTEM $restart
$BIN/v-restart-web $restart
$BIN/v-restart-proxy $restart

# Logging
log_history "disabled webmail access for $domain"
log_event "$OK" "$ARGUMENTS"

exit
