# Fail2Ban configuration file for devcp

[Definition]

actionstart = /usr/local/devcp/bin/v-add-firewall-chain <name>
actionstop = /usr/local/devcp/bin/v-delete-firewall-chain <name>
actioncheck = iptables -n -L INPUT | grep -q 'fail2ban-<name>[ \t]'
actionban = /usr/local/devcp/bin/v-add-firewall-ban <ip> <name>
actionunban = /usr/local/devcp/bin/v-delete-firewall-ban <ip> <name>
